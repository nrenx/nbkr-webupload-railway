{
  "build": "pip install -r requirements.txt && python -m playwright install chromium",
  "start": "python -m playwright install chromium && gunicorn app:app --bind 0.0.0.0:10000",
  "envs": {
    "PYTHON_VERSION": "3.9.0"
  }
}

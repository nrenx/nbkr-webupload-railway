{
  "build": {
    "command": "pip install -r requirements.txt && python -m playwright install chromium && python -m playwright install-deps"
  },
  "start": {
    "command": "gunicorn app:app --bind 0.0.0.0:10000"
  },
  "envs": {
    "PYTHON_VERSION": "3.9.0"
  }
}
